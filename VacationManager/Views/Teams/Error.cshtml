@model string
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Error</title>
</head>
<body>
    @if (Model == "Add" || Model == "Update")
    {
        <h1>You can not make this user the leader of this team, because the user already has a team</h1>
    }
</body>
</html>
